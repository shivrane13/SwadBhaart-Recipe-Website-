<!DOCTYPE html>
<html lang="en">
  <%- include("./head") %>
  <body>
    <div class="container-xxl px-md-5 bg-white shadow-lg" id="all-container">
      <header><%- include("./header") %></header>
      <main>
        <div class="px-4 py-5 my-5 text-center">
          <h1 class="display-5 fw-bold">Create Your Account</h1>
          <div class="col-lg-6 mx-auto">
            <p class="lead">
              Share your amazing recipes with thousand of People accross the
              world. Create an account and get started.
            </p>
          </div>
        </div>

        <div class="row justify-content-center">
          <div class="col-8">
            <% if(userExist != "") { %>
            <div class="alert alert-danger" role="alert"><%= userExist %></div>
            <% } else { if(submitSuccess != "") { %>
            <div class="alert alert-success" role="alert">
              <%= submitSuccess %>
            </div>
            <% } }%>
            <form action="create-account" method="post">
              <div class="row g-3">
                <div class="col-12">
                  <label for="name" class="form-lable">Your Name</label>
                  <input
                    type="text"
                    name="name"
                    id="name"
                    class="form-control"
                    required
                  />
                </div>

                <div class="col-12">
                  <label for="email" class="form-lable">Email</label>
                  <input
                    type="email"
                    name="email"
                    id="email"
                    class="form-control"
                    required
                  />
                </div>

                <div class="col-12">
                  <label for="password" class="form-lable">Password</label>
                  <input
                    type="text"
                    name="password"
                    id="password"
                    class="form-control"
                    required
                  />
                </div>

                <div class="col-12">
                  <label for="cpassword">Conform Password</label>
                  <input
                    type="text"
                    name="cpassword"
                    id="cpassword"
                    class="form-control"
                    required
                  />
                </div>

                <div>
                  <button type="submit" class="btn btn-primary">Sign Up</button>
                </div>
              </div>
            </form>
          </div>
        </div>
      </main>
      <footer class="py-5">
        <p>Build by Gaurav</p>
      </footer>
    </div>
  </body>
</html>
