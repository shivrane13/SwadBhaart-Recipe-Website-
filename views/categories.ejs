<!DOCTYPE html>
<html lang="en">
  <%- include('./head') %>
  <body>
    <div class="container-xxl px-md-5 bg-white shadow-lg" id="all-container">
      <header><%- include("./header") %></header>
      <main>
        <h1 class="pb-4">Explore Categories</h1>

        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Home</a></li>
            <li class="breadcrumb-item active" aria-current="page">
              Explore Categories
            </li>
          </ol>
        </nav>

        <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3 mb-4">
          <% if(categories != '') { %> <% for(const category of categories ) {%>

          <a
            href="/categories/<%= category.name %> "
            class="col text-center category__link"
          >
            <div class="category__img shadow">
              <img
                src="<%= category.image %>  "
                alt="<%= category.name %>  "
                loading="lazy"
              />
            </div>
            <div class="pt-1"><%= category.name %></div>
          </a>
          <% } %> <% } %>
        </div>
      </main>
      <footer class="py-5">
        <p>Build by Gaurav</p>
      </footer>
    </div>
  </body>
</html>
